{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "buildCommand": "docker build -t app ."
  },
  "deploy": {
    "startCommand": "dotnet Finlay.PharmaVigilance.API.dll",
    "restartPolicyMaxRetries": 5,
    "restartPolicyWindow": 600
  }
}
